#include "tree.h"

/* expand an exprmetic expression
 * ----------------------------------------------------------------------- */
union node*
expand_expr(struct nargarith* expr, union node** nptr, int flags) {
  union node* n = *nptr;

  return n;
}
