diff --git a/src/expand/expand_arith_expr.c b/src/expand/expand_arith_expr.c
index a4562ba4..609e9c5b 100644
--- a/src/expand/expand_arith_expr.c
+++ b/src/expand/expand_arith_expr.c
@@ -87,7 +87,6 @@ expand_arith_expr(union node* expr, int64* r) {
     case N_ARGPARAM: {
       union node *node, *n;
       stralloc* value;
-      size_t len = 0;
 
       node = tree_newnode(N_ARG);
       n = expand_param(&expr->nargparam, &node, 0);
@@ -96,14 +95,23 @@ expand_arith_expr(union node* expr, int64* r) {
       value = &n->narg.stra;
       assert(value);
 
-      ret = len == 0;
+      /* POSIX: an unset or empty variable is 0 in arithmetic context,
+         not an error -- without this, N_ARGPARAM's "no digits scanned"
+         case (len == 0, e.g. from a genuinely empty value->s) fell
+         through to the same ret = 1 as a real parse failure, which
+         expand_arith() treats as "expansion failed" and silently
+         produces no output at all instead of substituting the 0 this
+         is actually supposed to be. */
+      if(!value->s || value->len == 0) {
+        *r = 0;
+        ret = 0;
+      } else {
+        ret = scan_longlong(value->s, r) == 0;
 
-      if(n && value->s)
-        len = scan_longlong(value->s, r);
-      ret = len == 0;
+        if(ret)
+          *r = 0;
+      }
 
-      if(ret)
-        *r = 0;
       break;
     }
 
