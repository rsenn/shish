diff --git a/src/sh/sh_main.c b/src/sh/sh_main.c
index 21e67761..0f84bec7 100644
--- a/src/sh/sh_main.c
+++ b/src/sh/sh_main.c
@@ -35,7 +35,7 @@ sh_onsig(int signum) {
       int status;
       struct job* job = 0;
 
-      if(term_output) {
+      if(term_output && term_reading) {
         term_erase();
         term_restore(term_input.fd, &term_tcattr);
       }
@@ -63,7 +63,7 @@ sh_onsig(int signum) {
       buffer_putnlflush(fd_err->w);
 #endif
 
-      if(term_output) {
+      if(term_output && term_reading) {
         term_attr(term_input.fd, 1, &term_tcattr);
         prompt_number--;
         prompt_show();
diff --git a/src/term.h b/src/term.h
index 8ebc0087..094ca203 100644
--- a/src/term.h
+++ b/src/term.h
@@ -22,6 +22,14 @@ extern int term_insert;
 extern int term_dumb;
 extern unsigned long term_pos;
 extern buffer* term_output;
+/* set only while term_read() is actually blocked mid-line waiting on
+ * more terminal input (i.e. we're genuinely sitting at the prompt) --
+ * checked by sh_onsig()'s SIGCHLD handler before it erases/redraws
+ * the "current line": a job finishing while we're busy *executing* a
+ * command (any command, not just this specific one -- includes every
+ * child a command substitution's own pipeline forks) isn't a redraw
+ * situation, there's no in-progress prompt line to protect */
+extern volatile int term_reading;
 
 extern struct termios term_tcattr;
 extern struct winsize term_size;
diff --git a/src/term/term_read.c b/src/term/term_read.c
index b3b57043..dad4df61 100644
--- a/src/term/term_read.c
+++ b/src/term/term_read.c
@@ -9,6 +9,7 @@
 
 static char term_inbuf[BUFFER_INSIZE];
 buffer term_input = BUFFER_INIT(0, 0, term_inbuf, sizeof(term_inbuf));
+volatile int term_reading;
 
 /* ----------------------------------------------------------------------- */
 int
@@ -39,6 +40,8 @@ term_read(int fd, char* buf, unsigned int len) {
 
   prompt_show();
 
+  term_reading = 1;
+
   while((ret = buffer_getc(&term_input, &c)) > 0) {
 
 #ifdef DEBUG_OUTPUT_
@@ -83,6 +86,7 @@ term_read(int fd, char* buf, unsigned int len) {
     }
   }
 fail:
+  term_reading = 0;
 #ifdef DEBUG_OUTPUT_
   debug_ulong("term_read.ret", ret, 0);
   debug_nl();
